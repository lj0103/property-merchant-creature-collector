# 属性商人：精灵收集家

[![CI](https://github.com/lj0103/property-merchant-creature-collector/actions/workflows/ci.yml/badge.svg)](https://github.com/lj0103/property-merchant-creature-collector/actions/workflows/ci.yml)

一个受经典资源收集桌游启发的原创精灵收集策略卡牌小游戏。当前仓库已经从“本地同屏版”升级为“本地同屏 + 真实多人在线房间版”。

## 当前交付状态

已完成一个可以实际运行的多人在线版本：玩家可在不同浏览器/设备中连接同一台服务端，通过房间码加入同一房间，准备后开始游戏，并由服务端统一校验和广播所有游戏行动。

当前在线版使用安全游客身份：服务端通过 `HttpOnly + SameSite` Cookie 保存会话，生产环境自动启用 `Secure`，浏览器脚本无法读取会话 token。持久化支持本地 JSON 或 Prisma + PostgreSQL；配置 `REDIS_URL` 后启用跨实例 Socket.IO 广播、房间分布式锁和在线状态记录。

## 启动方式

安装依赖：

```bash
npm install
```

本地同屏模式：

```bash
npm run dev
```

线上多人模式需要同时启动前端和服务端：

```bash
npm run dev:online
```

也可以拆开启动：

```bash
npm run dev:server
npm run dev
```

默认地址：

- 前端：`http://localhost:5173`
- 联机服务端：`http://localhost:8787`
- 健康检查：`http://localhost:8787/health`

## 公网部署

仓库根目录包含 `render.yaml`，可在 Render 中通过 Blueprint 一次部署为同源应用：同一个 HTTPS 地址同时提供游戏页面、会话接口与 Socket.IO 长连接。生产前端会自动连接当前域名，无需配置 `VITE_SOCKET_URL`，也不会遇到跨域 Cookie 被浏览器拦截的问题。

Render 免费 Web Service 适合试玩和小规模好友对局：空闲一段时间后会休眠，首次访问唤醒可能需要约一分钟；免费实例文件系统不是永久存储，服务重启后未完成房间可能丢失。需要长期稳定运营时，应升级实例并配置 PostgreSQL，可选配 Redis 用于多实例协调。

如果前端和服务端不在同一台机器或不同域名，设置：

```bash
VITE_SOCKET_URL=http://你的服务端地址:8787
CLIENT_ORIGIN=http://你的前端地址:5173
```

数据库模式：

```bash
cp .env.example .env
npm run db:generate
npm run db:deploy
npm run dev:online
```

没有配置 `DATABASE_URL` 时，服务端会自动退回 JSON 文件存储，默认路径为 `server/data/rooms.json`。

Redis 协调模式（可选）：

```bash
REDIS_URL=redis://localhost:6379 npm run dev:online
```

没有配置 `REDIS_URL`，或启动时无法连接 Redis，服务端会自动退回进程内协调模式。访问 `/health` 可查看当前的 `storage` 与 `realtime` driver。

构建与测试：

```bash
npm run build
npm test
npm run test:integration
npm run test:all
```

`npm test` 运行快速单元测试；`npm run test:integration` 会启动临时服务端并执行真实 HTTP + WebSocket 双客户端流程；`npm run test:all` 依次运行两者。

## 阶段开发标注

### 阶段 1：本地同屏 MVP（已完成）

开发内容：

- 2–5 名玩家在同一浏览器中轮流游玩。
- 能量获取、预定、捕捉、支付、折扣、徽章、弃牌、最终轮和排名结算。
- 浏览器 `localStorage` 自动保存本地对局。
- 原创卡牌、徽章、元素符号和中文 UI。

主要代码：

- `src/game/types.ts`
- `src/game/setup.ts`
- `src/game/rules.ts`
- `src/store/gameStore.ts`
- `src/components/GameBoard.tsx`

### 阶段 2：规则引擎抽离（已完成）

开发内容：

- 新增 `src/game/actions.ts`。
- 将获取能量、预定卡牌、捕捉卡牌、归还能量等行动抽成纯规则函数。
- 本地 Zustand store 改为调用同一套行动规则。
- 服务端也使用同一套规则，避免客户端和服务端规则分叉。

验收结果：

- `npm test` 通过。
- `npm run build` 通过。

### 阶段 3：多人在线服务端（已完成）

开发内容：

- 新增 `server/index.ts`。
- 使用 Express + Socket.IO 实现长连接联机服务。
- 支持游客身份恢复、房间创建、房间加入、准备状态、房主开始游戏、离开房间、再来一局。
- 服务端权威执行游戏行动，客户端只提交意图。
- 支持行动幂等 `actionId`，减少重复提交造成的状态错误。
- 支持断线后进入 `reconnecting` 状态，超时后标记为 `offline`。
- 使用 JSON 文件保存 session、房间和对局快照，默认位置为 `server/data/rooms.json`。

环境变量：

- `PORT`：服务端端口，默认 `8787`。
- `CLIENT_ORIGIN`：允许跨域访问的前端源，默认 `http://localhost:5173`。
- `RECONNECT_GRACE_MS`：断线重连宽限时间，默认 `60000`。
- `ROOM_DATA_FILE`：房间快照文件路径。

验收结果：

- `npm run server` 可启动服务端。
- `/health` 返回 `{"ok":true,...}`。

### 阶段 4：多人在线前端（已完成）

开发内容：

- 首页新增“进入线上多人房间”入口。
- 新增 `src/components/OnlineGame.tsx`。
- 支持设置昵称、创建房间、加入房间、展示房间码、玩家准备、房主开局。
- 在线对局中显示玩家在线状态、当前回合、公共能量池、市场、玩家手牌/预定区、日志和结算弹窗。
- 所有在线行动通过 Socket.IO 发送到服务端，由服务端校验后广播房间状态。
- 页面刷新后使用本地保存的游客 token 恢复身份和房间。

主要代码：

- `src/components/OnlineGame.tsx`
- `src/multiplayer/protocol.ts`
- `src/components/CreatureCard.tsx`
- `src/App.tsx`

### 阶段 5：文档与运行说明（已完成）

开发内容：

- README 补充本地/联机启动方式。
- README 标注每个阶段的功能范围、主要代码和验收结果。
- 新增真实多人在线完整开发规格文档：[ONLINE_MULTIPLAYER_SPEC.md](./ONLINE_MULTIPLAYER_SPEC.md)。

### 阶段 6：PostgreSQL + Prisma 持久化（已完成）

开发内容：

- 新增 `prisma/schema.prisma`，定义游客会话和房间快照表。
- 新增 `prisma/migrations/20260709122000_init/migration.sql`，用于生产环境迁移。
- 新增 `server/storage.ts`，将持久化层抽象为 JSON / Prisma 两种 driver。
- 服务端启动时自动判断：有 `DATABASE_URL` 且未设置 `STORAGE_DRIVER=json` 时使用 Prisma；否则使用 JSON。
- `/health` 返回当前存储 driver，便于部署后确认运行模式。
- 新增 `.env.example`，标注前端、服务端和数据库环境变量。

主要代码：

- `server/storage.ts`
- `prisma/schema.prisma`
- `prisma/migrations/20260709122000_init/migration.sql`

验收结果：

- `npm run db:generate` 通过。
- `npm test` 通过。
- `npm run build` 通过。

### 阶段 7：Redis 实时协调与房间并发控制（已完成）

开发内容：

- 新增 `server/realtime.ts`，封装 Redis / 内存两种实时协调 driver。
- 配置 `REDIS_URL` 后启用 Socket.IO Redis adapter，使房间事件可跨服务端实例广播。
- 为加入、准备、开局、游戏行动、重开、离开和断线状态更新增加房间级串行锁。
- Redis 模式使用带随机令牌和过期时间的分布式锁，并用 Lua 脚本安全释放锁。
- 记录玩家 `online`、`reconnecting`、`offline` 状态，并设置在线状态 TTL。
- 各服务端实例通过内部 `room:sync` 事件同步房间快照缓存。
- Redis 缺失或连接失败时自动使用进程内队列锁，不影响单机开发和单实例部署。
- `/health` 新增 `realtime` 字段，可确认当前运行在 `redis` 或 `memory` 模式。

环境变量：

- `REDIS_URL`：Redis 连接地址。
- `REDIS_LOCK_TTL_MS`：房间分布式锁过期时间，默认 `5000`。
- `REDIS_PRESENCE_TTL_SECONDS`：在线状态过期时间，默认 `120`。

主要代码：

- `server/realtime.ts`
- `server/index.ts`

验收结果：

- `npm test` 通过。
- `npm run build` 通过。
- 无 Redis 环境下服务端可正常启动，`/health` 返回 `"realtime":"memory"`。

### 阶段 8：安全游客会话与跨实例会话查询（已完成）

开发内容：

- 新增 `POST /api/session`，由服务端创建或恢复游客身份并写入 `HttpOnly` Cookie。
- Cookie 使用 `SameSite=Lax`；生产环境或 `COOKIE_SECURE=true` 时启用 `Secure`。
- Socket.IO 握手增加 Cookie 鉴权，未认证连接无法调用房间和游戏事件。
- WebSocket 握手校验 `Origin`，拒绝非 `CLIENT_ORIGIN` 的浏览器连接。
- 前端先建立安全 HTTP 会话，再使用 `withCredentials` 建立实时连接。
- 会话 token 不再返回给前端，也不再保存到 localStorage。
- 支持把旧版 localStorage token 一次性迁移至安全 Cookie，迁移后立即删除旧 token。
- 持久化层新增按 token 查询和保存会话接口；Prisma 模式可在任意服务实例按需恢复共享会话。
- 昵称更新和断线状态继续由服务端持久化，并与房间状态同步。

环境变量：

- `SESSION_COOKIE_NAME`：会话 Cookie 名称，默认 `pm_session`。
- `SESSION_COOKIE_MAX_AGE_MS`：会话有效期，默认 30 天。
- `COOKIE_SECURE`：本地 HTTP 开发设为 `false`；正式 HTTPS 部署设为 `true`。

主要代码：

- `server/index.ts`
- `server/storage.ts`
- `src/components/OnlineGame.tsx`
- `src/multiplayer/protocol.ts`

安全说明：

- 生产环境必须使用 HTTPS，并让浏览器访问的前端域名与 Cookie 策略匹配。
- `CLIENT_ORIGIN` 必须设置为真实前端地址，不能在携带凭据时使用通配符来源。

验收结果：

- `npm test` 通过。
- `npm run build` 通过。
- 未携带 Cookie 的 Socket.IO 连接返回 `UNAUTHORIZED`。
- 双 HttpOnly Cookie 客户端建房、加入、准备、开局和游戏行动流程通过。
- `/api/session` 响应不包含会话 token。

### 阶段 9：服务端集成测试与 GitHub Actions（已完成）

开发内容：

- 新增 `server/integration.test.ts`，测试时自动启动隔离的临时服务端和 JSON 数据目录。
- 自动验证未认证 Socket.IO 连接返回 `UNAUTHORIZED`。
- 自动验证恶意 WebSocket `Origin` 无法连接。
- 自动验证 `/api/session` 设置 HttpOnly Cookie 且响应不泄露 token。
- 使用两个独立 Cookie 客户端执行身份恢复、建房、加入、准备、开局和首回合行动。
- 新增 `test:integration` 和 `test:all` 脚本，区分快速单元测试与真实网络集成测试。
- 修复服务端退出时未清理断线重连计时器的问题，现在可以优雅停止。
- 新增 `.github/workflows/ci.yml`，每次推送 `main` 或提交 Pull Request 时自动安装依赖、运行全部测试并构建。

主要代码：

- `server/integration.test.ts`
- `.github/workflows/ci.yml`
- `server/index.ts`
- `package.json`

验收结果：

- 单元测试：3 个测试文件、5 项测试通过。
- 集成测试：1 个测试文件、完整双客户端场景通过。
- `npm run build` 通过。
- 临时服务端在测试结束后可正常退出，无残留计时器。

### 阶段 10：五人房间与行动输入校验（已完成）

开发内容：

- 本地同屏和线上房间人数上限由 4 人扩展为 5 人。
- 五人对局每种基础能量初始化为 8 枚，万能能量保持 5 枚。
- 五人对局随机提供 6 枚候选徽章。
- 前端新增“5 人”选项和第五位玩家昵称输入框。
- 服务端、共享协议、持久化类型和完整开发规格统一支持 `maxPlayers=5`。
- 新增真实五客户端集成测试，验证加入、准备、开局、玩家状态和能量池同步。
- 服务端新增游戏行动 Zod 运行时校验，拒绝伪造的能量类型、卡牌来源和无效行动结构。

验收结果：

- 五人规则初始化单元测试通过。
- 五客户端线上房间集成测试通过。
- 非法游戏行动返回 `BAD_REQUEST`。
- `npm run test:all` 与 `npm run build` 通过。

### 阶段 11：沉浸式实体桌游 UI（已完成）

设计目标：

- 从传统后台面板式布局升级为多人围坐的实体桌游桌面。
- 保持全部本地与线上游戏逻辑不变，只重构信息层级、布局与视觉反馈。

开发内容：

- 使用深色木纹背景、墨绿桌毡、金属包边和桌面刻字营造实体长桌质感。
- 玩家席位排列在桌面远端并形成轻微弧形，当前行动席拥有灯光提示，线上模式额外标记自己的座位。
- 公共能量改成带厚度与阴影的实体筹码，五种属性拥有独立材质色彩。
- 精灵市场改成中央公共牌区，牌库呈现为叠放牌堆，卡牌使用羊皮纸、属性插画框和声望角标。
- 当前玩家信息改成靠近屏幕下方的个人玩家垫，集中展示筹码、羁绊、预定牌和桌边旅记。
- 首页与线上大厅同步改造成桌游盒封面和开桌准备区视觉。
- 增加桌面、平板和手机断点；窄屏隐藏横向滚动条并保持玩家席位可滑动。
- 重构 `SetupScreen`、`GameBoard` 与 `CreatureCard`，提升结构语义和后续维护性。

主要代码：

- `src/styles/globals.css`
- `src/components/SetupScreen.tsx`
- `src/components/GameBoard.tsx`
- `src/components/OnlineGame.tsx`
- `src/components/CreatureCard.tsx`

验收结果：

- 1280px 桌面视口视觉验收通过。
- 390px 手机视口无横向页面溢出。
- 浏览器控制台无错误或警告。
- `npm run test:all` 与 `npm run build` 通过。

### 阶段 12：宝石化筹码与桌面端一屏棋盘（已完成）

设计目标：

- 桌面端无需滚动即可同时看到公共市场、目标徽章、公共宝石和玩家已持有宝石。
- 将所有适合用宝石表达的资源统一为带切面的彩色宝石造型。

开发内容：

- 公共能量区改名为公共宝石区，五种基础资源与万能资源使用不同颜色的切面宝石。
- 玩家持有资源改为两排宝石陈列，数量与宝石造型紧邻显示。
- 永久羁绊折扣、卡牌费用和目标徽章同步使用小型宝石造型。
- 桌面端固定为三栏布局，左侧目标与公共宝石、中央三层市场、右侧玩家宝石与个人区域始终同时可见。
- 三层市场按视口剩余高度等分，卡牌内部内容随高度紧凑排列。
- 桌面端锁定为视口高度并隐藏页面滚动；手机和平板仍使用响应式分区布局。
- 对 1280×720 与 1920×1080 两种桌面视口进行了实际浏览器尺寸验证。

验收结果：

- 1280×720 页面 `scrollHeight` 等于 `720`，三层市场与 6 个目标徽章全部可见。
- 1920×1080 页面 `scrollHeight` 等于 `1080`，完整桌面处于视口范围内。
- 持有宝石、公共宝石和永久折扣无需滚动即可查看。
- 页面无横向溢出，浏览器控制台无错误或警告。
- `npm run test:all` 与 `npm run build` 通过。

### 阶段 13：多形状宝石与目标可视化（已完成）

开发内容：

- 六种资源使用六种不同轮廓：八边形焰宝石、六边形潮宝石、水滴形森宝石、祖母绿切割雷宝石、菱形念宝石和圆形万能宝石。
- 不同轮廓统一应用于公共宝石、玩家持有宝石、永久折扣和卡牌费用。
- 新增 `GemDisplay` 组件，集中管理宝石图标、数量、无障碍标签和中文属性映射。
- 徽章达成条件由文字缩写改为对应颜色、形状和数量的宝石组合。
- 徽章条件和卡牌费用共用统一的宝石图标与数量组件。
- 1600px 以上桌面适当放大左右信息区和徽章条件宝石，同时继续保持一屏棋盘。

验收结果：

- 浏览器计算样式确认六种资源拥有六种不同的 `clip-path`。
- 1280×720 下徽章条件宝石和三层市场同时可见，页面无滚动。
- 浏览器控制台无错误或警告。
- `npm run test:all` 与 `npm run build` 通过。

### 阶段 14：万能宝石说明与无行动保护（已完成）

开发内容：

- 万能宝石区域明确显示“不可直接拿取 · 预定时获得”。
- 悬停万能宝石可查看完整说明：预定公开精灵卡时，公共区若有万能宝石则自动获得 1 枚。
- 公共普通宝石耗尽时，界面提示玩家改为捕捉或预定精灵，而不是误认为游戏卡死。
- 新增服务端权威 `passTurn` 行动；仅当玩家无法拿宝石、无法捕捉且无法继续预定时允许跳过。
- 本地模式和线上模式共用同一套无合法行动判断，客户端不能自行绕过规则。
- 规则引擎仍会记录玩家因无可执行行动而跳过回合，界面不再逐条展开历史行动。

验收结果：

- 正常仍有行动时，跳过回合会被规则引擎和服务端拒绝。
- 构造公共宝石耗尽、市场无牌、预定区已满且无法购买的状态时，可以安全进入下一位玩家回合。
- 万能宝石说明在 1280×720 一屏布局中可见，页面无额外滚动。
- `npm run test:all` 与 `npm run build` 通过。

### 阶段 15：玩家宝石汇总与数字防遮挡（已完成）

开发内容：

- “桌边旅记”由逐轮行动流水改为玩家宝石总览，每名玩家仅显示当前持有的不同颜色宝石及数量。
- 数量为零的颜色不占用桌面空间；没有宝石的玩家显示“暂无宝石”，当前行动玩家使用高亮标记。
- 公共宝石区改为“宝石图形、名称、数量”三列布局，数量固定在右侧独立区域。
- 缩小公共区六种宝石的最大尺寸，并针对桌面一屏断点单独限制宽高，避免特殊形状覆盖名称或数量。
- 徽章条件、卡牌费用和玩家汇总中的数字移到宝石轮廓外侧，不再被 `clip-path` 裁切。
- 新增玩家宝石汇总组件测试，验证非零颜色、数量、空状态和当前玩家信息可正确渲染。

验收结果：

- TypeScript、客户端与服务端完整构建通过。
- 玩家宝石汇总组件测试通过，零数量不会生成冗余宝石。
- 公共宝石图形与数量拥有独立网格列，CSS 层面消除重叠区域。
- `npm test` 共 8 项组件与规则测试通过，`npm run build` 通过。

### 阶段 16：五行灵珠与羁绊可视化（已完成）

开发内容：

- 资源主题统一调整为火、水、木、金、土五行灵珠，以及可代替任意元素支付的万能灵珠。
- 六种灵珠全部改为球形造型，通过红、蓝、绿、金、棕、紫六种材质和珠内汉字区分元素。
- “桌边旅记”为每位玩家增加“持”和“绊”两行：“持”表示捕捉时会消耗且计入 10 枚上限的持有灵珠，“绊”表示来自已捕捉精灵、不会消耗的永久折扣。
- 当前玩家区域增加区别说明与悬停提示，避免把永久羁绊误认为另一组可消耗灵珠。
- 市集卡牌中央直接显示对应元素灵珠，并标注捕捉后会获得哪一种永久羁绊。
- 金、土属性的精灵名称、描述和旅者徽章名称同步改写，使内容与五行主题一致。
- 所有操作提示、错误提示、卡牌费用和公共资源文案由“能量/宝石”统一为“灵珠”。

验收结果：

- 玩家汇总测试覆盖持有灵珠、永久羁绊、万能灵珠和零数量隐藏逻辑。
- 现有 8 项组件与规则测试全部通过。
- TypeScript、客户端和服务端生产构建通过。

### 阶段 17：桌面信息完整性与牌库扩容（已完成）

开发内容：

- 桌面端持有灵珠固定为等宽等高的正圆，永久羁绊灵珠同步使用正圆造型。
- 预定区改成三张紧凑卡片纵向排列，完整展示卡牌等级、声望、名称、费用和捕捉按钮，不再截断第二、第三张预定牌。
- 旅者徽章由两列改为一列，每个徽章独占一行，完整展示名称和全部达成条件。
- 徽章区新增“仅统计永久羁绊”提示；条件前显示“绊”标志，避免误用持有灵珠计算。
- 远端玩家席位缩小高度、头像和文字，取消容易遮挡的倾斜位移；当前玩家使用更醒目的金色双边框与灯光。
- 牌库采用经典《璀璨宝石》的发展卡规模：等级 1 为 40 张、等级 2 为 30 张、等级 3 为 20 张，共 90 张。
- 市集标题实时显示各等级“剩余牌量 / 总牌量”，剩余牌量同时包含牌库和四张公开牌。
- 增加牌库耗尽保护：当三个等级的牌库和市集全部为空时自动进入最终轮，完成本轮后按现有排名规则结算。

验收结果：

- 新增牌库数量、徽章条件来源和牌库耗尽最终轮测试。
- 预定区最多三张卡片在桌面端无需内部滚动即可完整显示。
- 现有组件、规则和生产构建测试通过。

### 阶段 18：自适应表单与折扣规则提示（已完成）

开发内容：

- 首页改为独立纵向滚动容器；选择四人或五人时，即使屏幕高度不足也能向下查看并点击“开始游戏”。
- 解除桌面断点对整个页面的全局滚动锁定，只在实际游戏棋盘中保持一屏布局。
- 徽章条件灵珠支持自动换行，并在窄侧栏中缩小条件灵珠，五行旅者等多条件徽章可完整展示。
- 每张市集卡和预定卡的顶部增加属性灵珠，直接表示捕捉后获得的永久羁绊颜色。
- 市集与预定区明确标注购买顺序：永久羁绊先自动抵扣对应费用，再用持有灵珠和万能灵珠支付余额。
- 卡牌显示的是“羁绊抵扣后所需灵珠”，按钮不足提示同步改为“折后灵珠不足”。

验收结果：

- 首页五人姓名表单和开始按钮可通过纵向滚动完整访问。
- 多条件徽章不会再从徽章卡片右侧被裁切。
- 预定卡缩略图可直接看到卡牌代表的永久羁绊颜色。
- 购买计算继续复用统一的 `getEffectiveCost`、`canCapture` 和 `paymentFor` 规则。

### 阶段 19：五人首页一屏适配（已完成）

开发内容：

- 为高度不超过 820px 的桌面屏幕增加紧凑首页断点，重点适配 1280×720 视口。
- 等比例缩小封面标志、游戏标题、介绍间距、人数按钮、席位输入框和开始按钮。
- 五名玩家继续使用两列三行布局，无需滚动即可看到全部席位和“开始游戏”。
- 高度低于 680px 时进一步隐藏介绍文字并压缩输入框，优先保证核心操作完整可见。
- 首页保留极端尺寸下的滚动能力，但隐藏可见滚动条，避免破坏桌游封面观感。

验收结果：

- 两人至五人切换不改变开始按钮的可访问性。
- 游戏棋盘与线上大厅断点不受首页紧凑规则影响。
- TypeScript、组件测试和生产构建通过。

### 阶段 20：灵珠色值统一（已完成）

开发内容：

- 六种灵珠固定使用同一套元素主色：火 `#d84a32`、水 `#238ec8`、木 `#31955b`、金 `#d5a632`、土 `#9b663c`、灵 `#8d69c5`。
- 公共灵珠、持有灵珠、永久羁绊、徽章条件、卡牌费用和卡牌属性标识共用同一条球体材质规则。
- 统一高光位置、白色反光、暗部混色、内阴影和外投影，消除相同元素在不同区域出现深浅偏差的问题。
- 删除公共区、玩家区和小型图标各自独立的渐变参数，后续调整色值只需修改元素主色和统一材质规则。

验收结果：

- 同一种元素在所有资源组件中使用完全相同的 CSS 主色与渐变参数。
- TypeScript、组件测试和生产构建通过。

### 阶段 21：桌面首页无滚动布局（已完成）

开发内容：

- 桌面首页不再等待矮屏断点，默认启用紧凑布局，并关闭本地开桌页的纵向滚动。
- 开桌选择框宽度由 620px 进一步收窄到 580px，同时压缩内边距。
- 三人和五人姓名输入区改为三列布局，五名玩家由三行减少为两行。
- 两人和四人继续使用两列布局，保持输入框宽度与可读性。
- 手机端和线上房间页面不使用该规则，继续保留各自的响应式布局。

验收结果：

- 桌面端选择五人时，全部姓名输入框与开始按钮位于同一屏内。
- 本地开桌首页不再出现或依赖纵向滚动。
- TypeScript、组件测试和生产构建通过。

### 阶段 22：连续对局与胜场记录（已完成）

开发内容：

- 游戏状态新增连续对局统计，记录本次桌局已经完成的局数以及每位玩家累计胜场。
- 每局结算时自动将总局数加一，并为获胜玩家增加一场胜利；并列获胜时每位并列玩家各记一胜。
- 游戏顶部常驻显示当前是第几局，以及所有玩家的累计胜场。
- 结算弹窗增加完整对局记录，本地模式提供“再来一局”和“结束本次对局并返回首页”两个明确操作。
- 本地“再来一局”沿用原玩家、玩家 ID 和累计战绩，只重置牌库、灵珠、徽章与本局分数。
- 线上房间重开后回到准备大厅，但保留上一局战绩；所有玩家准备后开始下一局并继续累计。
- 旧存档和旧房间没有统计字段时自动按 0 局、0 胜兼容，不影响继续游戏。

验收结果：

- 新增自动化测试验证结算增加局数、胜者增加胜场，并在第二局继续保留。
- 现有 11 项组件与规则测试全部通过。
- TypeScript、客户端和服务端生产构建通过。

### 阶段 23：新玩家规则说明弹窗（已完成）

开发内容：

- 首页、本地游戏桌面和线上游戏桌面新增“游戏规则”按钮，任何阶段都能打开说明。
- 规则按六个部分组织：回合行动、预定与万能灵珠、捕捉与羁绊、灵珠上限与徽章、最终轮与排名、连续对局记录。
- 明确说明预定不支付费用、羁绊先抵扣、持有灵珠支付余额、万能灵珠最后补缺的购买顺序。
- 明确徽章只统计永久羁绊，持有灵珠和万能灵珠不能代替。
- 规则弹窗支持关闭按钮、点击遮罩关闭和 `Esc` 键关闭，并为屏幕阅读器提供对话框语义。
- 弹窗内容区域可独立滚动，手机端自动缩小边框、间距和标题字号。

验收结果：

- 本地模式和线上模式均可随时查看完整规则。
- 弹窗不会改变当前游戏状态或中断正在进行的回合。
- 现有 11 项组件与规则测试全部通过，生产构建通过。

### 阶段 24：规则弹窗全屏修复与联机隔离（已完成）

开发内容：

- 规则弹窗改用 React Portal 直接挂载到页面根层，不再受游戏顶部栏的模糊效果和布局范围限制。
- 遮罩层固定覆盖完整浏览器视口，并提高层级；长规则内容继续在弹窗内部独立滚动，避免只显示顶部片段。
- 打开、关闭规则只使用当前浏览器中的组件本地状态，不写入共享游戏状态，也不发送 Socket.IO 事件。
- 线上多人模式中，每位玩家可以独立打开或关闭规则；查看规则不会影响其他玩家界面、当前回合或任何游戏操作。

验收结果：

- 从游戏桌面顶部点击“游戏规则”后，弹窗完整居中覆盖当前视口。
- 同一线上房间内，一名玩家查看或关闭规则不会同步到其他玩家。
- TypeScript、现有组件与规则测试以及客户端和服务端生产构建通过。

### 阶段 25：公网同源部署（已完成）

开发内容：

- 增加 Render Blueprint，使用一个 Node Web Service 同时承载 React 静态页面、HTTP API 和 Socket.IO 服务。
- 生产环境由 Express 提供构建后的 `dist` 目录，并为前端路由返回入口页面。
- 生产前端默认连接当前页面域名，本地开发仍默认连接 `http://localhost:8787`。
- 前端、会话 Cookie 与 WebSocket 使用同一 HTTPS 域名，避免第三方 Cookie 和跨域凭据兼容问题。
- Blueprint 配置新加坡区域、健康检查、主分支自动部署和免费实例规格；生产构建显式安装 Vite、TypeScript、Prisma 与 tsx 等构建工具。

验收结果：

- 客户端与服务端生产构建通过。
- HTTP 会话和两个独立 Socket.IO 客户端的真实房间流程测试通过。
- 部署后可通过一个公开 HTTPS 地址在不同设备和网络中创建、加入并进行同一场对局。

## 游戏规则

每回合选择一项行动：获取三种不同五行灵珠、在公共池至少有四枚时获取两枚同种灵珠、预定一张公开精灵卡，或捕捉一张公开/已预定的精灵卡。

- 已捕捉精灵永久提供对应五行属性 1 点羁绊折扣；永久羁绊不会消耗，也不计入持有灵珠上限。
- 购买公开卡或预定卡时会同时使用羁绊和灵珠：先用永久羁绊自动减少同色费用，再消耗持有的同色灵珠，最后由万能灵珠补足仍缺少的颜色。
- 旅者徽章只统计对应颜色的永久羁绊数量，持有灵珠和万能灵珠都不能代替徽章条件；捕捉精灵后满足条件会自动获得徽章。
- 灵珠是万能资源，不能直接从公共区拿取；预定公开精灵卡时，公共区若有余量会自动获得 1 枚，并在捕捉时自动补足缺少的元素。
- 预定最多三张，并在公共池有余量时获得一枚万能灵珠。
- 持有灵珠超过十枚时必须立即归还至十枚。
- 公共五行灵珠耗尽时，玩家仍可捕捉或预定精灵；只有所有合法行动都无法执行时才能跳过回合。
- 捕捉后若满足条件，会自动获得尚未归属的徽章。
- 任一玩家达到 15 分后完成当前轮，再按分数结算。
- 若所有等级牌库和市集均已耗尽，即使无人达到 15 分，也会完成当前轮后按当前声望结算。
- 平分依次比较：捕捉卡较少、万能灵珠较少；仍相同则并列获胜。

## 技术结构

- Vite + React + TypeScript
- Zustand：本地同屏状态管理与持久化
- Express + Socket.IO：多人在线房间服务
- Prisma + PostgreSQL：生产数据库持久化
- Redis：跨实例事件广播、在线状态与房间分布式锁
- Zod：服务端输入校验
- `src/game`：初始化、规则、计分、行动执行等纯逻辑
- `src/multiplayer`：前后端共享联机协议类型
- `src/data`：原创精灵卡与徽章配置
- `src/components`：本地游戏、线上房间和卡牌 UI
- `server`：HTTP + WebSocket 多人服务端

## 当前在线版能力边界

已经支持真实跨设备联机，并已具备 Redis 多实例协调基础：

- 不配置 Redis 时适合本机、局域网或单台云服务器部署。
- 默认房间快照持久化到 JSON 文件；配置 `DATABASE_URL` 后使用 PostgreSQL。
- 配置 Redis 后可跨实例广播事件、同步房间缓存并串行处理同一房间操作。
- 游客 token 只保存在服务端签发的 HttpOnly Cookie 中，前端 JavaScript 不可读取。
- Prisma 模式支持共享会话按需查询；正式多实例部署应同时使用 PostgreSQL + Redis，并在负载均衡器启用 WebSocket。
- 当前仍是匿名游客身份，不包含邮箱注册、密码找回或第三方登录。

## 后续生产化阶段

建议按以下顺序继续：

1. 增加限流、房间自动清理和结构化日志。
2. 增加前端浏览器端到端测试与断线重连压力测试。
3. 增加生产部署配置和监控告警。
4. 部署前端静态站点与独立 WebSocket 服务。
5. 如需长期账号，再增加邮箱或第三方登录体系。

本项目中的名称、视觉符号与文案均为原创，不使用任何第三方角色 IP 素材。
