import { CREATURE_CARDS } from '../data/cards'; import { BADGES } from '../data/badges'; import { EMPTY_ENERGIES, POOL_BY_PLAYERS } from '../data/constants'; import type { CreatureCard, GameState, Level, Player } from './types';
export const shuffle=<T,>(items:T[])=>{const a=[...items];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a;};
export const createGame=(names:string[]):GameState=>{const players:Player[]=names.map((name,i)=>({id:`p${i+1}`,name:name.trim()||`玩家 ${i+1}`,energies:EMPTY_ENERGIES(),capturedCards:[],reservedCards:[],badges:[],turns:0}));const decks={} as Record<Level,CreatureCard[]>;const market={} as Record<Level,CreatureCard[]>;for(const l of [1,2,3] as Level[]){const d=shuffle(CREATURE_CARDS.filter(c=>c.level===l));market[l]=d.splice(0,4);decks[l]=d;}const n=POOL_BY_PLAYERS[names.length];return{players,currentPlayerIndex:0,energyPool:{flame:n,aqua:n,leaf:n,spark:n,mind:n,wild:5},decks,market,availableBadges:shuffle(BADGES).slice(0,names.length+1),phase:'playing',finalRoundTriggered:false,winnerIds:[],log:[{id:crypto.randomUUID(),message:`旅程开始，${players[0].name} 先行动。`}]};};
