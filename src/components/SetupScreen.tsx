import { useState } from 'react'; import { useGameStore } from '../store/gameStore';
export function SetupScreen(){const start=useGameStore(s=>s.startGame);const hasSave=useGameStore(s=>s.hasSave);const [count,setCount]=useState(2);const [names,setNames]=useState(['玩家 1','玩家 2','玩家 3','玩家 4']);return <main className="setup"><div className="setup-card"><div className="brand-mark">✦</div><p className="eyebrow">原创策略卡牌游戏</p><h1>属性商人<br/><span>精灵收集家</span></h1><p className="intro">采集五相能量，与奇妙生灵缔结羁绊。率先抵达 15 分，并在最终轮守住你的荣光。</p><div className="count-pills">{[2,3,4].map(n=><button className={count===n?'active':''} onClick={()=>setCount(n)} key={n}>{n} 位玩家</button>)}</div><div className="name-grid">{names.slice(0,count).map((name,i)=><label key={i}><span>收集家 {i+1}</span><input value={name} maxLength={12} onChange={e=>setNames(v=>v.map((x,j)=>j===i?e.target.value:x))}/></label>)}</div><button className="primary big" onClick={()=>start(names.slice(0,count))}>开启旅程 <span>→</span></button>{hasSave&&<p className="save-hint">检测到存档，当前对局已自动恢复。</p>}</div></main>}
